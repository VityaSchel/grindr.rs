{
	"name": "grindr-rs-config",
	"private": true,
	"description": "Config syncing only. This crate ships no JavaScript.",
	"scripts": {
		"sync-config": "opengrind-config sync",
		"check:config": "opengrind-config check"
	},
	"devDependencies": {
		"@opengrind/config": "^1.3.0",
		"lefthook": "^2.1.10",
		"prettier": "^3.9.6"
	},
	"type": "module",
	"prettier": "@opengrind/config/prettier"
}
